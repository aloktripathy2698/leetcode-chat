{
  "name": "leetcode-assistant-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix apps/extension",
    "build": "npm run build --prefix apps/extension",
    "lint": "npm run lint --prefix apps/extension"
  }
}
